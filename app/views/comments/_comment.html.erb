<div class="comment">
  <p>
    <% if comment.user %>
      <%= link_to comment.user.display_name, user_path(comment.user) %>
    <% else %>
      Anonymous
    <% end %>
  </p>
  <p>
  <strong>Comment:</strong>
  <%= comment.content %>
  </p>
</div>
